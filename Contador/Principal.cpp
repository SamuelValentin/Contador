#include "header.h"

int main(){
    Contador a;

    a.getCount();
    a.setCount(4);
    a.getCount();
    a.incrementa();
    a.getCount();
    a.decrementa();
    a.getCount();

    return 0;
}